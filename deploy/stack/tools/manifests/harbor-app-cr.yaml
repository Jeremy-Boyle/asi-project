#@ load("@ytt:data", "data")
---
apiVersion: kappctrl.k14s.io/v1alpha1
kind: App
metadata:
  name: #@ "{}-harbor".format(data.values.cluster_name)
  namespace: #@ data.values.namespace
spec:
  cluster:
    kubeconfigSecretRef:
      name: #@ "{}-kubeconfig".format(data.values.cluster_name)
      key: value
  syncPeriod: 5m
  paused: false
  fetch:
  - helmChart:
      name: harbor
      repository:
        url: https://charts.bitnami.com/bitnami
      version: "11.2.3"

  template:
  - helmTemplate:
      name: harbor
      namespace: harbor
      valuesFrom:
      - secretRef:
          name: #@ "{}-harbor-helm-values".format(data.values.cluster_name)
  - ytt:
      ignoreUnknownComments: true
      inline:
        paths:
          00-namespace.yaml: |
            #@ load("@ytt:overlay", "overlay")
            ---
            apiVersion: v1
            kind: Namespace
            metadata:
              labels:
                kubernetes.io/metadata.name: harbor
              name: harbor
            ---
            #@ servicemonitor = overlay.subset({"kind": "ServiceMonitor"})
            #@ namespace = overlay.subset({"kind": "Namespace"})
            #@ crd = overlay.subset({"kind": "CustomResourceDefinition"})
            #@overlay/match by=overlay.not_op(overlay.or_op(servicemonitor, namespace, crd)),expects="0+"
            ---
            #@overlay/match missing_ok=True
            metadata:
              #@overlay/match missing_ok=True
              namespace: harbor

  deploy:
  - kapp:
      rawOptions:
      - --wait-timeout=5m
      - --diff-changes=true